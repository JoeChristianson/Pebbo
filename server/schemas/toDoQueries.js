const {ToDoForm} = require("../models/ToDoForm")
const {User} = require("../models")

const toDoQueries = {
    // getSubTasks: async (parent,{userId,toDoId})=>{
    //     const user = await User.findById(userId).populate;

    // }
}

module.exports = toDoQueries