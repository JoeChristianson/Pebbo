

const queueItemActive = (day,queueItem,queue)=>{
    try{
        console.log(day)
        console.log(queueItem)
        console.log(queue)
    }catch(err){
        console.log(err)
    }
}

module.exports = {queueItemActive}